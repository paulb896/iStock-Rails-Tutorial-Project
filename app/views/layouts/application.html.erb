<!DOCTYPE html>
<html>
<head>
  <title>iStock Rails Tutorial Project</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	<div id="user_information">
		<% if logged_in? %>
			<p><%= "Welcome #{current_user.username}!" %></p>
		<% else %>
			<%= link_to 'Login', login_path %>
		<% end %> 
	</div>
	<%= yield %>

</body>
</html>
